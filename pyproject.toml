[project]
name = "best-restaurants"
version = "0.2.0"
description = "Pull Yelp's full ranked list of restaurants near any address or city"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
    "pandas>=2.0",
    "playwright>=1.45",
    "seaborn>=0.13",
]

[tool.uv]
package = false
